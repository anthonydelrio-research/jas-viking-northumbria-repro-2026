# GIS Dependencies for Spatial Data Validation
# Required for run_jas_analysis.py and supporting spatial analysis scripts
#
# Installation:
#   pip install -r requirements-gis.txt
#
# Platform notes:
#   - macOS: May require GDAL installation via homebrew first:
#     brew install gdal
#   - Linux: Install GDAL development packages:
#     apt-get install gdal-bin libgdal-dev
#   - Windows: Consider using conda for easier GDAL dependency management

# Core dependencies (pinned versions from 2025-12-17)
geopandas==1.1.1
rasterio==1.4.4
pandas==2.2.3
numpy==2.2.4
shapely==2.1.2
pyproj==3.7.1

# Fiona is a geopandas dependency but pinning for reproducibility
fiona>=1.9.0,<2.0.0
